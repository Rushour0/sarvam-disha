# Copy to scripts/deploy.env and fill in. deploy.env is untracked on purpose:
# host names and key paths must never land in the repo.
DISHA_DEPLOY_HOST="ec2-user@your-host.compute.amazonaws.com"
DISHA_DEPLOY_KEY="$HOME/path/to/your-key.pem"
